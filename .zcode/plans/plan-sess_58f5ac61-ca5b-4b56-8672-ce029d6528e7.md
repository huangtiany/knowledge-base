# AI/Agent 模块知识体系落地计划

本计划把访谈收敛的全部决策一次落地。共识摘要：定位混合偏应用；交付物 = 扩充标签清单 + 一篇《AI / Agent 知识地图》；示例内容全删；标签粒度 = 章；学习主线 Python → RAG → Agent（LLM基础穿插、评测横切），主线以项目实战收尾，**微调为最末尾的扩展方向，不属于主线**；文章分「笔记型 / 整理型」两类文体；不以任何语言为叙事框架，类比只用 JS/TS。

---

## 1. 删除示例内容

| 删除对象 | 说明 |
|---|---|
| `content/ai/llm-basics/attention-notes.md` | 搭建期示例（用户已确认，含读起来像真笔记的两篇 AI 文章） |
| `content/ai/rag/embedding-notes.md` | 同上 |
| `content/stack/language/java-generics.md` | stack 域示例 |
| `content/stack/language/img/`（整目录） | 仅被上文的示例引用 |

- 删除后清掉空目录：`content/ai/llm-basics/`、`content/ai/rag/`、`content/stack/language/`
- **保留**：`DESIGN.md`、`TECH-ARCHITECTURE.md`、`DEV-PLAN.md`、`README.md`（约定文档，不是示例）
- 两个 `resources.yaml` 文件保留，仅清空卡片（见第 4 步）
- 构建安全性已核实：文章 collection 容忍零文章（栏目页/首页有空态分支，M4 已验收）

## 2. `content/tags.yaml`：ai 组扩成 10 个标签

```yaml
ai:
  - 路线图          # 新增：仅知识地图文章使用
  - Python          # 新增：章1
  - LLM基础         # 保留：章2（支线，穿插）
  - RAG             # 保留：章3
  - Agent设计模式    # 新增：章4
  - Agent框架       # 保留：章4
  - 工具调用/MCP     # 保留：章4
  - 评测与可观测     # 新增：章5（横切）
  - 项目实战         # 保留：章6
  - 微调            # 保留：章7（扩展方向，主线之外，保持"待学习"灰组）
stack:              # 不动
  - 语言基础 / JVM / Spring生态 / 数据库 / 中间件 / 工程实践 / 项目实战
```

顺序即栏目页分组显示顺序：路线图最前作入口，主线章按学习顺序排，**微调沉到最后**，与「主线之外」的定位一致。

## 3. 新增 `content/ai/roadmap.md`：《AI / Agent 知识地图》

frontmatter：

```yaml
---
title: AI / Agent 知识地图
date: 2026-09-05
tags: [路线图]
summary: AI/Agent 模块的知识体系、学习主线与写作约定——写成一篇笔记，点亮一个链接。
---
```

正文结构（h2 自动获得 § 编号，进 TOC）：

1. **定位与主线**：一句话定位（混合偏应用：先会用 → 能解释 → 遇瓶颈再下钻）；主线 Python → RAG → Agent，LLM基础穿插，评测横切，主线以实战收尾；微调在主线之外，是后续扩展方向
2. **七个章**（每章 = 一个 h2，挂对应标签说明）：
   - 章1 Python（`Python`）：基础段（整体语法地图）→ 进阶段（uv/类型注解/dataclass与pydantic/装饰器/pytest）→ 工程段（asyncio/pydantic校验/FastAPI/numpy-pandas/PyTorch读懂级，嵌入 RAG 实战）
   - 章2 LLM基础（`LLM基础`）：推理侧优先（采样参数/上下文窗口/KV cache与量化/结构化输出），训练侧只留全景一篇
   - 章3 RAG（`RAG`）：基础管线（chunking/embedding选型/向量库/检索与重排/生成与引用）+ 前沿专题（agentic RAG/GraphRAG/长上下文vs RAG/多模态）
   - 章4 Agent（`Agent设计模式`+`Agent框架`+`工具调用/MCP`）：设计模式（workflow vs agent、五个基础模式、手写最小循环、记忆、多智能体）→ 工具调用与 MCP → LangGraph 深入（对照阅读不开笔记：OpenAI Agents SDK、CrewAI）
   - 章5 评测与可观测（`评测与可观测`）：RAGAS、Agent evals、Langfuse（笔记多半被章3/4实战催生）
   - 章6 项目实战（`项目实战`）：实战一「格致问答」（语料=本站 content/，FastAPI+流式+引用溯源）；实战二「工具 Agent」（检索+MCP 工具）；站上只记过程/踩坑/复盘
   - 章7 微调·扩展方向（`微调`）：**主线之外的后续扩展**——LoRA/QLoRA、embedding 微调、数据集准备，遇到真实需求再解锁；清单里保持「待学习」灰组，作为路线图的自然延伸
3. **写作与资源约定**：两类文体（笔记型=自己写、短、敢留开放问题；整理型=AI 整理、过目修订后入库）；不以其他语言为叙事框架，类比只用 JS/TS（如 typing↔TS 类型系统、pydantic↔zod、asyncio↔JS async/await）；资源卡只收 GitHub 仓库与官方文档/外部文档
4. **进度点亮机制说明**：规划主题一律先写纯文本；写成对应笔记后回地图把文本替换为互链（relbox 自动收录）

## 4. 清空资源卡（两文件保留、内容留 `[]`）

`content/ai/resources.yaml` 与 `content/stack/resources.yaml` 各替换为「标题注释 + `[]`」。**必须是空数组**：resources loader 对纯注释/空文件解析出 `null` 会直接抛错（`content.config.ts:61` 已核实）；`YAML.parse("[]")` 返回 `[]`，校验通过。

## 5. `DESIGN.md` 同步

「初始标签清单」一节：ai 组更新为现行 10 标签（微调列在末位），并加一行注记「现行清单以 `content/tags.yaml` 为准，2026-09-05 随知识体系扩充」。stack 组不动。

## 6. `README.md` 写作约定补充

「写作约定速查」末尾加小节「内容约定」（3–4 行）：文章分笔记型/整理型两类文体；类比只用 JS/TS、不做"从 X 语言转来"的叙事框架；规划中的笔记先在知识地图中以纯文本存在，写成后回地图点亮互链。

## 7. 构建验证与走查

- `npm run build` 必须干净通过（标签校验、空 collection、空资源数组三处都要过）
- `npm run dev` 快速走查：首页空态 + 路线图出现在最近更新；AI 栏目页 = 1 个「路线图」组（1 篇）+ 9 个灰色「待学习」组（微调组排在最后）；首页子主题门户 = 1 实 chip + 9 虚线 chip；资源页空态正常；文章页 § 编号/TOC/样式正常

## 明确不做

- 不写任何章内笔记（那是后续学习过程，本计划只搭体系）
- 不动 stack 域标签与文档结构（`java-generics` 删除后 stack 域暂为空，属预期）
- 不提交 git：改动留在工作区，走查确认后由你决定提交（或告诉我来提交）
