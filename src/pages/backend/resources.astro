---
import { getCollection } from 'astro:content';
import ResourceList from '../../components/ResourceList.astro';

const resources = await getCollection('backendResources');
---
<ResourceList dom="backend" resources={resources} />
