---
import { getCollection } from 'astro:content';
import ResourceList from '../../components/ResourceList.astro';

const resources = await getCollection('frontResources');
---
<ResourceList dom="front" resources={resources} />
