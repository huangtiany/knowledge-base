---
import { getCollection } from 'astro:content';
import ResourceList from '../../components/ResourceList.astro';

const resources = await getCollection('stackResources');
---
<ResourceList dom="stack" resources={resources} />
