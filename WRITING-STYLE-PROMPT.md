# 知识库文风优化提示词

本文件是给改写执行者（人或 AI）的任务提示词。用途：逐篇优化本仓库 `content/` 下文章的文字表述，消除 AI 生成痕迹。文风问题，不改知识内容。

## 使用方式

每次处理一篇文章。执行指令模板：

> 按 `WRITING-STYLE-PROMPT.md` 的规则改写 `content/<域>/<目录>/<文件>.md`。只改文字表述，不增删知识点，不改代码、链接路径、frontmatter 的 date/tags/related。改完按文件末尾的自查清单过一遍。

全部文章改完后统一跑 `npm run build` 验证（互链与 frontmatter 校验），再跑 `node scripts/check-external-links.mjs` 确认外链无变化。

## 背景

「格致」是个人知识库，作者是资深前端工程师。读者是自己和同行：不需要被吸引点开，需要快速拿到结论、判断和可查的细节。好的笔记标准是：**结论前置、判断有依据、陈述平实**。

## 总原则

1. 像给同事讲清楚一件事那样写，不像在做演示。
2. 技术密度优先：每个段落都应该有信息增量，删掉只有节奏功能的句子。
3. 观点直说：倾向性结论（如「新项目默认 Vite」）是知识库的价值，保留并保留其语气。
4. 改写对象是表述方式，不是知识内容：不新增小节、不删小节、不改代码。

## 禁止项

### 一、模板框架句（本站最高频问题，优先处理）

以下句式外壳全部删除，把外壳里的内容还原成普通陈述句：

- 「……一句话：**……**」「心智两句话」「思想一句话」「一句话对照」「选型一句话」——站内 20+ 处
- 「收拢成一张表/三句话/一句」「收敛到……这一层」
- 「这篇按……的主线走」「这篇把……收拢成一篇」「这篇是……的速查」——每篇开头都不需要自我介绍，直接进入主题
- 「小结」一节只保留本节的可操作结论；删除「至此闭环」「至此打通」式的收尾感叹

写法对照：

- 原：「两代模型一句话：**Webpack 全量打包换兼容与确定性，Vite 原生 ESM 按需编译换开发体验**。」
- 改：「Webpack 全量打包，换来兼容性和确定性；Vite 开发态按需编译，换来更快的启动和热更新。」

### 二、俏皮话与比喻堆砌

一篇最多保留一个比喻（选解释力最强的那个，例如后端域的「前端类比」核心类比），其余改为直述。

清除清单（含本站实例）：

| 类型 | 实例（摘自本站） | 处理 |
|---|---|---|
| 口语噱头 | 吃饭的家伙、玄学、正确打开方式、脏活、劝退、翻车点 | 改直述，见替换表 |
| 拟物堆砌 | 三张底牌、两把刀、四级火箭、门票、心跳、眼睛、地基、账单/对账、装眼睛 | 删比喻，直接说事物本身 |
| 排比口号 | 「结构清楚 → 读得懂 → 变好 → 改得动」式的四连箭头升华 | 拆成一句平实的因果说明，或删 |
| 网络流行语 | 元凶、拉满、拿捏、神器 | 替换为平实词 |

### 三、空洞赞美与宏大词汇

出现即替换或删除，不做装饰性使用：

| 原词 | 替换 |
|---|---|
| 史无前例地平 | 上手门槛低 |
| 代差优势 | 明显优于 |
| 天花板 | 上限 |
| 十年沉淀 | 生态成熟 |
| 颠覆、革命性、极致、完美 | 删除，或按实际程度写（「显著」「可靠」「够用」） |
| 赋能、抓手、强强联合 | 删除 |
| 闭环 | 保留于工程语义（反馈回路），删除于口号用法（「至此闭环」） |

判断标准：删掉这个词，句子信息量不减，就删。

### 四、句式密度上限

AI 文风的特征不是某个句式，而是同一句式反复出现。每篇上限：

- 破折号「——」：≤ 2 处（本站多篇文章 5-14 处，多数应改冒号、逗号或拆句）
- 「不是 A，而是 B」：≤ 1 处
- 「X 的本质是 Y」「核心是 Y」类判断句：≤ 2 处
- 感叹号：0；emoji：0
- 引号强调（「」加粗）每篇 ≤ 3 处，只用于关键结论

### 五、标题与 summary

- 标题删掉比喻和口号：「组件通信：从 props 到 provide 的四级火箭」→「组件通信：props、插槽与依赖注入」；信息型标题（「HTTP 缓存与浏览器存储」「从 Webpack 到 Vite」）不动
- frontmatter 的 summary 同样按本规则改写：它是列表页副标题，去掉修辞，保留内容概括

## 保留项（不要过度修改）

1. 技术结论、倾向性建议、具体数字、版本号、达标线——一律不动。
2. 代码块、表格、有序/无序列表的结构——不动；列表项内的文字可按规则改写。
3. 互链：`[显示文字](路径.md)` 的路径部分绝对不动，显示文字可微调。
4. frontmatter 的 date、tags、related 不动。
5. 后端域文章的「前端类比」教学法是有意设计：类比框架保留，只删类比之外的装饰性修辞。
6. 各域 `roadmap.md` 的功能说明（章节结构、留白说明、进度点亮）只需按规则去修辞，结构不动。
7. 站点定位语句（domains.ts 的 desc、About 页）属于改写范围：改成平实版本，保留原意。

## 硬约束（改完必须成立）

- `npm run build` 通过（互链死链检查、标签校验）。
- 知识点数量不变：改写前后，每篇文章能回答的技术问题清单一致。
- 所有代码示例、命令、路径、字段名原样保留。

## 自查清单（每篇改完过一遍）

1. 「一句话」「收拢成」「这篇按……主线」类框架句已清零？
2. 比喻 ≤ 1 个？俏皮话清零？
3. 宏大词（史无前例/代差/天花板/极致/完美）清零？
4. 破折号 ≤ 2 处？「不是 A 而是 B」≤ 1 处？无感叹号？
5. 每个小节结尾是可操作结论，还是口号？口号已改？
6. 代码、链接路径、表格结构、frontmatter 关键字段未被改动？
7. 知识点无增删？

## 改写示例（均摘自本站原文）

### 示例一：summary 去宏大词与破折号

改前：

> summary: 移动跨端的两条路线：RN 用 JS 驱动原生组件（Expo 让上手曲线史无前例地平），Flutter 用 Dart 自绘像素（三端像素级一致）——机制、上手与选型清单。

改后：

> summary: 移动跨端的两条路线：RN 用 JS 驱动原生组件，Flutter 用 Dart 自绘像素。机制差异、上手方式与选型清单。

### 示例二：比喻段落改直述

改前（javascript/01 开头）：

> 这三个概念是面试与实际 bug 的最大重合区：防抖函数靠闭包，`this` 指向错乱源于作用域与绑定规则，`instanceof` 失灵源于原型链。天天在用，但很多环节「知其然不知其所以然」。这篇按「变量怎么找到 → 函数怎么记住 → 对象怎么继承」的顺序把三张底牌理清楚。

改后：

> 作用域、闭包与原型链分别回答三个问题：变量在哪可见、函数为何能访问外部变量、属性沿什么路径查找。三者对应工程里的高频问题：防抖函数的实现、回调里 `this` 丢失、`instanceof` 判断失灵。下文按此顺序展开。

### 示例三：小结口号改结论

改前（html-css/02 小结）：

> 布局决策树收拢成一句：**一维 Flex、二维 Grid、组件级响应式用容器查询、连续值用 clamp**。剩下的坑大多来自盒模型与 min-width 这类底层细节。

改后：

> 一维布局用 Flex，二维面板用 Grid，组件级响应式用容器查询，连续缩放值用 clamp。常见的布局问题多与盒模型和 `min-width` 有关。
