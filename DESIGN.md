# 个人知识库网站 · 需求/设计文档

> 版本：v1.0（2026-09-05）
> 状态：定稿。本文件是项目的第一份文档，记录产品层面的完整设计共识，不含技术选型。

## 一句话定位

一个**纯展示型**的个人知识库网站：内容全部在本地以 Markdown 写好，网站是这些文件的展示门面——只负责**存**（组织好）和**展示**（让自己和任何访客方便浏览）。任何修改都发生在本地文件里，网站没有编辑、没有管理后台。它陪着前端、后端与 AI/Agent 三条主线，从零开始长。

## 核心决策

| 决策点 | 结论 |
|---|---|
| 受众 | 主要给自己，完全公开，别人看到是顺便的事 |
| 网站职责边界 | 纯展示。正文与标签的唯一修改入口是本地项目文件，改完同步即更新（展示最新版，无版本概念） |
| 内容类型 | ① 文章/笔记：本地写好的 Markdown；② 资源卡：外部链接 + 一句话摘要，不存原文 |
| 顶层结构 | 三个一级栏目：**AI/Agent**、**后端**、**前端**。领域内不做预设子目录，靠标签组织，结构从内容里长出来 |
| 标签治理 | 受控标签表：本地维护一份标签清单，打标只从清单选，加新标签先进清单。它不是网站功能，是写作约定，兼任学习路线图 |
| 首页 | 动态门户：子主题由标签自动生成，零内容时只有三个领域入口，随内容长成门户 |
| 找内容 | 栏目导航 + 站内搜索 + 标签筛选 |
| 资源卡呈现 | 每个领域一个独立的"资源收藏"页，与文章分开；卡面按受控标签分组（组内按收集时间倒序），标签是可选字段，未打标落「未分组」 |
| 宽屏布局 | 页面容器 1412px：正文阅读列宽不变（列表页 1080px），侧栏住进两侧空白——栏目页右侧标签目录、文章页左「全部笔记」右 TOC；侧栏 sticky、限高内滚 |
| 移动端布局 | 侧栏按断点收起（<1280 收左栏、<900 收右栏），收起后由右下角「目录」抽屉接管：栏目/资源页为标签目录，文章页为「本页目录 / 全部笔记」双 tab |
| Markdown 渲染 | 代码块语法高亮（Java/Python 至少）、图片、LaTeX 数学公式、长文 TOC、文内互链 |
| 身份 | About 页：简短介绍 + GitHub 等链接（信息展示，非互动邀请） |
| 互动 | 无评论区、无任何反馈渠道，纯单向展示 |
| 回到顶部 | 全站右下角固定按钮：滚过约 0.6 屏出现，原生 JS 平滑滚动，尊重 prefers-reduced-motion |
| 成功标准 | 不设——长期存在本身就是目的 |

## 页面清单

- **首页**：站名 + 一句话介绍 + 三个领域入口 + 标签生成的子主题门户 + 最近更新
- **栏目页 ×3**（AI/Agent、后端、前端）：按标签分组展示文章，右侧标签目录（锚点 + scroll-spy）
- **文章页**：正文 + TOC + 标签 + 文内链接；左「全部笔记」栏（当前文章高亮）+ 文末上一篇/下一篇（沿学习路径链）
- **资源收藏页 ×3**：按标签分组的链接卡时间流 + 右侧标签目录
- **About 页**
- **全站搜索**
- **移动端目录抽屉**：侧栏被断点收起后的替代入口

## 标签清单

AI/Agent：`路线图`、`Python`、`LLM基础`、`RAG`、`Agent设计模式`、`Agent框架`、`工具调用/MCP`、`Agent生态`、`评测与可观测`、`项目实战`、`微调`

后端：`路线图`、`Java基础`、`数据库`、`JavaWeb`、`Spring生态`、`中间件`、`工程实践`、`项目实战`、`JVM`

前端：`路线图`、`HTML与CSS`、`JavaScript`、`TypeScript`、`浏览器原理`、`Vue生态`、`React生态`、`工程化`、`性能优化`、`项目实战`、`移动与跨端`

> 使用规则：打标只从清单选；想用新标签，先加进清单再使用。清单随时可增删。
> 现行清单以 `content/tags.yaml` 为准；2026-09-05 随知识体系扩充（AI/Agent 组原初始六标签、全栈组原初始七标签），见《AI / Agent 知识地图》与《后端知识地图》；2026-09-06「全栈开发」栏目更名「后端」并新增前端组，见《后端知识地图》与《前端知识地图》。

## 明确不做的

在线编辑、草稿流程、标签管理后台、评论区、最低质量门槛、预设子目录、存量迁移。
